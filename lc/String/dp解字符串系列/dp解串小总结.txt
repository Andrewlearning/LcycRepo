

首先dp数组的初始化， 往往是子串的长度+1， 这是为什么呢？
因为往往dp有个状态转移的过程，例如 dp[i][j] = dp[i-1][j-1] + 1
那么因为有这样一个的转移过程 所以就要求dp的数组，长度一定要大于原数组，要不然+1 , -1这样就会产生越界
所以一般情况下都是 len(dp) = len(array) + 1

上面这个规律，适用于所有dp的题目
--------


然后从dp 和 array的对应情况来看， 一般是 dp[1] 对应的是 array[0]
就是例如 dp[1][1] 对应的是 array1[0] 和 array2[0]这两个子串